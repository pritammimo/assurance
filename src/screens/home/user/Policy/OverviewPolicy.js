import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const overviewPolicy = () => {
  return (
    <View>
      <Text>Overview Policy</Text>
    </View>
  );
};

export default overviewPolicy;

const styles = StyleSheet.create({});
