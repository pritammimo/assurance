import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const SendMessges = () => {
  return (
    <View>
      <Text>Send Message</Text>
    </View>
  );
};

export default SendMessges;

const styles = StyleSheet.create({});
